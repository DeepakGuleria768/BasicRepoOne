fun main(args : Array<String>) : Unit{
	// The first line is defining a function called a main()
	// fun keyword is used to define the main() function
	// Unit means return nothing(it is optional)
	// main() method is the entry point of the program it executes first when program run
    println("Hello world !")
        // calling println() function with string arguments
	// in kotlin; are optional
}
